name = "cx"
description = "LLM context management"
managed = true

[binary]
name = "cx"
path = "./bin/cx"

[context]
default_rules = "dev-no-tests"

[[hooks.on_stop]]
name = "Format Go files with gofumpt"
command = "gofumpt -w ."
run_if = "changes"
