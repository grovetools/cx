# Documentation Task: Contributing Guide

Write a guide for developers who want to contribute to the `grove-context` project.

## Task
- Refer to `Makefile` and `CLAUDE.md` for build and test instructions.
- List the main development commands: `make build`, `make test`, `make test-e2e`, `make lint`.
- Mention that `golangci-lint` is used for linting and can be configured in `.golangci.yml`.
- Briefly describe the code contribution process (e.g., fork, create a branch, submit a pull request).

## Context Files to Read
- `Makefile`
- `CLAUDE.md`
- `.golangci.yml`
- `.github/workflows/ci.yml`