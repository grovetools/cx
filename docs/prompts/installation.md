# Documentation Task: Installation Guide

Write a concise installation guide for `grove-context` (`cx`).

## Task
- Explain that the recommended installation method is through the Grove meta-CLI.
- Provide the exact command: `grove install context`.
- Mention any prerequisites, such as having the `grove` CLI installed.
- Do not include build-from-source instructions. Keep it simple for end-users.

## Context Files to Read
- `README.md`